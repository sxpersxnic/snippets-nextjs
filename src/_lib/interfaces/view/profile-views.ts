export default interface ProfileView {
  username: string;
  photo: string;
  description: string;
}

export default interface ProfilePreview {
  id: string;
  username: string;
  photo: string;
}